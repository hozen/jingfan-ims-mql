# 2026-02-17 - HBS Tutor App Development

## Major Changes

### Disk Space Management
- Moved entire `hbs_tutor_app` to external USB drive: `/Volumes/MacOS_Disk/hbs_tutor_app/`
- Created symlink in workspace: `hbs_tutor_app -> /Volumes/MacOS_Disk/hbs_tutor_app/`
- Reason: User experiencing disk shortage, videos are ~14GB

### Video Asset Issues
- `hbsvideo` folder relocated to `large_assets/hbsvideo/` on external drive
- **Critical**: Many video files are 0 bytes due to iCloud Drive copy failures
- iCloud error: "Resource deadlock avoided" / "Unknown system error -11"
- Terminal commands (cp, rsync, cat) cannot reliably copy iCloud Drive files
- **Solution**: User must manually drag-drop from iCloud Drive in Finder
- Zero-byte files detected in Module_2, Module_3, Module_4, Module_5, Module_7

### iOS Build Constraints
- iOS apps **cannot** use symlinks to external drives in app bundle
- Xcode build fails if hbsvideo is symlinked outside project
- Capacitor `npx cap sync ios` copies www assets to `ios/App/App/public/`
- Videos must be physically present in ios folder for device deployment

### GitHub Backup
- Created private repo: `hozen/hbs_tutor_app`
- .gitignore excludes: node_modules/, large_assets/, *.mp4, www/hbsvideo/
- Initial commit: 26 files (source code only, no videos)

## Technical Fixes Applied
- Fixed homepage flex layout for full-screen height
- Fixed iOS overscroll white/blank areas (removed `overscroll-behavior-y: none`)
- Fixed page overlap bug (`.active` class on `#page-home`)
- Set dark theme background `#0f172a` in capacitor.config.json and AppDelegate.swift
- Fixed header safe-area padding with `calc(10px + env(safe-area-inset-top))`
- Set `overflow: hidden` on body for offline app (no pull-to-refresh needed)

## Pending Tasks
- [ ] Re-download 0-byte video files from iCloud Drive manually via Finder
- [ ] Complete Module_2 video sync (some files still missing/corrupted)
- [ ] Test app on iPhone after videos are restored
- [ ] Consider moving large_assets back to www/hbsvideo after iCloud fix

## Key Paths
- iCloud source: `/Users/leishi/Library/Mobile Documents/com~apple~CloudDocs/02_HBS_finance/hbsvideo/`
- App location: `/Volumes/MacOS_Disk/hbs_tutor_app/`
- Videos: `/Volumes/MacOS_Disk/hbs_tutor_app/large_assets/hbsvideo/`
- Workspace symlink: `~/.openclaw/workspace/hbs_tutor_app`
